name: "100 · Validate Prerequisites"

@@SCAFFOLD_HEADER@@
on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  validate:
    runs-on: self-hosted
    steps:
      - name: Blocked on AWS account setup
        run: |
          echo "::error::100-validate-prereqs cannot run yet: it assumes the OIDC deploy role (secret AWS_DEPLOY_ROLE_ARN) to check the account, and that role does not exist (REVIEW.md R-003)."
          echo "::error::When it does, this job mirrors the Azure appliance's preflight: required secrets and variables present, role assumable, Bedrock model access enabled (REVIEW.md R-005), Docker Hub pull access. Implementation: TODO.md T-102."
          exit 1
