# Changelog

All notable changes to the Cloud Network Assessment (CNA) Platform will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## [Unreleased]

### Added
- Implemented the approved 0.9.0 polish plan (`CNA-0.90-updates.md` §3), covering the full web app and the API. **Theme repair (A):** the ~12 dark-only pages (all five presentation pages, report, connections, documents, findings, diagram, analysis, deliverables, client-deliverables, cloud-credentials) converted to proper dual-mode using the dashboard/FinOps idiom; the executive risk-gauge score now renders `currentColor` instead of white-on-white; chart grid/label colors moved to theme-aware CSS variables (`--chart-grid`/`--chart-text`); charts inherit Aeonik instead of forcing `sans-serif`; Copilot's nonexistent `text-warm-*` classes corrected to `text-warmgray-*`; the report cover logo swaps dark-teal/bright-teal SVGs per theme; off-brand blue retired from the submit button, spinner, help modal, auth error page, admin AI page, and global error page; the new-engagement form restyled from default Tailwind to the glass system. **Experience upgrades (B):** route-level glass skeleton `loading.tsx` for the dashboard group and engagement journey; `@tailwindcss/typography` installed so Copilot answers render structured; a shared `EmptyState` component (card + inline variants) replaced the drifted empty states; a demo-day runbook was produced as a session deliverable. **Behavioral hazards (C):** confirmation dialogs on the five previously one-click deletes (shared `ConfirmSubmitButton`); the Findings page no longer runs a destructive `deleteMany` dedupe on GET — the cleanup moved to a helper invoked when discovery starts, with a non-destructive in-memory display dedupe; six server actions and two raw mono dumps stopped echoing raw SDK/exception text (full errors now log server-side); generated deliverables get neutral "section pending review" text instead of embedded error messages; FinOps `[VERIFY]` tokens became a single dagger footnote; the dead Future-state toggle was removed; AWS "coming soon" copy softened. **API/Python hygiene (D):** `cna --version` reports the real project version via `importlib.metadata` (was hardcoded 1.0.0); `/intake` and `/publish` return honest 501s; error paths that returned HTTP 200 with an `error` body now use real status codes (422/404/502/503) with sanitized messages; a loud startup warning fires when `DATABASE_URL` is unset. Full Python suite green (705 passed); `next build` and `tsc --noEmit` clean.
- Brought `azure_discovery.py` from 22% to 85% coverage and removed the last `[tool.coverage.run] omit` entry, closing `TODO.md` → T-408. Both discovery orchestrators are now measured by the `fail_under = 80` gate — 1,569 statements that had never been covered by it, in the code T-403 called the highest-risk in the package. Package coverage went **up**, 84% → 87%, because both files landed above the package average; the 13-point shortfall the original estimate predicted assumed they stayed where they were. New tests cover the orchestrator's fault-isolation contract (one failing collector must not lose the subscription), the five phase-2 scans, every resource collector, and the Azure Monitor metrics group.
- Brought `aws_discovery.py` from 46% to 98% coverage and removed it from the `[tool.coverage.run] omit` list (`TODO.md` → T-408). It had been excluded on the stated premise that it "requires live AWS API calls against real tenants", which was false — its tests are fully mocked and make no network call — so the `fail_under = 80` gate had never measured 380 statements in what T-403 called the highest-risk code in the package. 46 new tests cover the collectors that had none. Total package coverage rises 84% → 87.5%, since the file now sits above the package average. `azure_discovery.py` received the same treatment (see the entry above): both discovery orchestrators are now measured by the gate.
- Expanded the Azure resource-provider registration check in `100-validate-prereqs.yml` from 2 namespaces to the 10 the Azure roots actually need (`TODO.md` → T-104). An unregistered provider does not fail the plan — it fails partway through apply, ~20 minutes in, leaving a half-provisioned environment; the check now turns that into a 30-second failure naming the exact `az provider register` command. `Microsoft.Cdn`, `Microsoft.CognitiveServices`, and `Microsoft.DBforPostgreSQL` are the notable additions, none of which is registered by default on a fresh subscription. The list is derived from the declared resource types and lives in one `REQUIRED_PROVIDERS` declaration rather than inline. `Registering` is now distinguished from `NotRegistered`, since advising a re-register on an in-flight registration is wrong. `Microsoft.AlertsManagement` is deliberately absent: the split refactor removed alerting entirely, so `REVIEW.md` → R-007 has been closed — it was asking the subscription owner for something no longer required.
- Implemented `EngagementStore.write_findings_report()` and `.write_deliverable_manifest()` (`TODO.md` → T-412). Both were already called — by `AnalysisEngine.run()` and `RenderPipeline.run()` respectively — against a class that did not define them, so each engine built a complete result and then died with `AttributeError` on its final statement. They write `reports/findings_report.json` and `reports/deliverable_manifest.json` through the existing `_atomic_write`, whose `default=str` covers the enums and datetimes `FindingsReport.model_dump()` leaves in place, and create `reports/` if a caller skipped `init()`. Filenames are class constants alongside the existing ones. The eight read-side methods are still missing, so the five scaffold CLI guards are unchanged; these two writers now pin down the on-disk layout those readers must match. Five tests against a real store on `tmp_path`.
- Declared ten previously-undeclared imports as a `[project.optional-dependencies] enrichment` extra (`TODO.md` → T-414): the eight Azure management SDKs plus `cairosvg` and `mcp`. They were imported lazily but declared nowhere, so a clean install could never activate Log Analytics diagnostics, alert counts, cost queries, private DNS and Compute enrichment, Defender posture, SVG→PNG export, or the MCP clients. An extra rather than core dependencies, matching the existing lazy-import behaviour. Bounds are verified against the code, not just latest-installable — `managementgroups` 2.x, `monitor` 7.x, and `costmanagement` 5.x each break an API this code calls, so upper bounds are set on all ten.
- Added `tests/unit/test_persistence.py` (`TODO.md` → T-410). `EngagementStore` had no test file, which is how three separate signature bugs survived a green suite — the rest of the suite mocks the store, and a `MagicMock` accepts any method name, keyword, or arity. Ten tests against a real store on `tmp_path`, covering init/save/load round-trips, audit events, and discovery checkpoints, with explicit guards that the two previously-wrong call shapes now raise `TypeError`.
- Added `ty` to the `dev` dependency group with a `[tool.ty]` section, and appended a `ty` hook to the existing `.pre-commit-config.yaml` (`TODO.md` → T-402). The hook is `repo: local` / `language: system` so it runs the version pinned in `pyproject.toml`; the 13 hooks already there were left untouched. It is registered at `stages: [manual]` rather than gating, because `ty check cna/` reports 96 diagnostics and T-402 forbids widening ignore lists to force a green first run — the backlog is tracked as T-410 and fixed instead.
- Added a `terraform-checks` job to `300-test-codebase.yml` (`TODO.md` → T-407). The AWS Terraform had no `fmt` or `validate` coverage anywhere — the only `terraform` invocations were in the Azure deploy and drift workflows, and `212-deploy-aws-split.yml` fails fast before Terraform runs, leaving 90 resources across eight modules unlinted. `terraform fmt -check -recursive` now covers both clouds on every push and PR, needing no provider download, no AWS account, and no credentials. Four pre-existing formatting violations were fixed to make the check pass: alignment in both workload `main.tf` files, the `alarm_thresholds` object in the observability module, and `security/locals.tf`. A companion `terraform validate` step was attempted and removed after it failed on its first run — the cause could not be diagnosed (Actions API unreadable to the automation, and `registry.terraform.io` blocked by egress policy, so neither the log nor a local reproduction was available). It is tracked as T-409 with the open question recorded: the AWS roots carry no `.terraform.lock.hcl` while the Azure roots all do.
- Added `TestRun` coverage for both discovery orchestrator entry points (`TODO.md` → T-403), 9 cases in `tests/unit/test_aws_discovery.py` and 10 in `tests/unit/test_azure_discovery.py`. `AWSDiscovery.run()` and `AzureDiscovery.run()` had zero coverage and are now at 100% (21/21 and 44/44 statements). Every cloud SDK collaborator is mocked, so no AWS or ARM call is made. Beyond the happy paths, the tests assert the retry contract (`get_caller_identity` retried on `CNARateLimitError`, then exhausting its budget), `CNAAuthError` propagating rather than returning an empty topology, Azure's network-only-Reader fallback when `subscriptions.get()` fails, `--resume` checkpoint skipping, and blocked subscriptions still being recorded. Filed T-408 for the related finding that `[tool.coverage.run] omit` excludes both files from the `fail_under = 80` gate on a false premise.
- Added `scripts/validate_documentation_model.py` and a `Validate documentation model` step to the `repository-guardrails` job in `300-test-codebase.yml` — CI now fails if a markdown file appears outside the four-document model (`TODO.md` → T-603). Vendored agent configuration (`.claude/`, `.agents/`, `.codex/`) is excluded, and platform-required `.github/` documents are pre-allowed.
- Added `tests/unit/test_guardrail_scripts.py` — regression coverage for the three validators in the `repository-guardrails` CI job, which previously had none (`TODO.md` → T-406). Each case builds a fixture repository in `tmp_path` and invokes the script as a subprocess, so the real entry point is exercised, and includes an explicit cwd-independence test for the `validate_module_deps.py` defect below.
- Added a project-neutral `documentation-curator` agent to the vendored agent pack (`.claude/agents/documentation-curator.md`). It reads the documentation model a repository's `README.md` declares and enforces placement, cross-reference integrity, staleness, and single-source-of-truth on documentation changes — usable here and drop-in reusable elsewhere, per the pack's project-neutrality rule (`TODO.md` → T-604).
- Added Architecture Decision Records ADR-0001 through ADR-0005 to the GitHub Wiki, covering the Anthropic removal, smoke-test removal, Entra redirect-URI ownership, Key Vault network hardening target, and the container image-pull strategy.
- Added `REVIEW.md` — the repository-wide record of blockers that require a human decision, approval, or access grant, with an owner and a required action for each.
- Added `TODO.md` — the authoritative engineering work queue, phased and dependency-ordered for engineer handoff.
- Added AWS Terraform under `infra/terraform/providers/aws/` (eight modules mirroring the Azure module boundaries) and `infra/terraform/environments/aws/{dev,prod}/{platform,workload}/`, declaring ECS Fargate, ALB, RDS PostgreSQL, S3, Secrets Manager, CloudFront + WAFv2, IAM/OIDC, KMS, CloudWatch, X-Ray tracing, Bedrock inference profiles, nine VPC endpoints, and Application Auto Scaling with scale-to-zero. Code generation only — nothing has been applied.
- Made the Foundry/AIServices account region configurable via a `foundry_location` workload variable (default `eastus2`) instead of a hardcoded literal.
- Wired live MCP endpoint defaults for Azure and AWS recommendation enrichment through Terraform variables, `.env.example`, and the existing GitHub variable bootstrap flow.
- Added streamable HTTP JSON-RPC tool-call support for the Azure and AWS MCP clients while preserving offline recommendation fallback when endpoints are unavailable.
- Extended CISA ZTMM v2 mappings for AWS WAF, Azure Application Gateway WAF, and Azure Front Door WAF findings into the `Applications and Workloads` pillar.
- Added regression coverage for live MCP JSON-RPC request shaping and WAF `Applications and Workloads` framework mappings.
- Added a break-glass local admin login (`/local-admin`, isolated from Entra ID SSO), PBKDF2-HMAC-SHA256 password hashing, and the supporting Terraform/Key Vault/migrator wiring so an environment can be signed into if Entra ID SSO is ever unavailable.

### Fixed
- Made secret scanning a control that actually gates (`TODO.md` → T-411). Three problems were closed. **(1)** The `detect-secrets` pre-commit hook excluded only `.env.example`, so all 8,480 vendored agent-pack files were in scope and produced 413 of the 446 findings — noise that made the baseline untriageable. `.claude/`, `.agents/`, `.codex/`, and `.deployment-catalog/` are now excluded by path, the same directories already excluded from ruff and the documentation model on the same grounds. That takes the count to 33. **(2)** All 33 were audited by hand and **none is a real secret** — they are `.example` placeholders, Key Vault secret *names*, `${{ secrets.* }}` references, connection strings interpolated from variables, test fixtures, and the SHA-256 checksum of the runner tarball. The 16 "AWS Access Key" and 2 "Private Key" findings that drove the original priority are all in the vendored packs, not in project code. The verdicts are recorded per-finding in `TODO.md`. **(3)** A gating `detect-secrets` job was added to `300-test-codebase.yml`; previously the only secret-scan job was `gitleaks` with `continue-on-error: true`, so no secret scan could fail a build. The new job runs the hook through pre-commit so the exclusion list is not restated and left to drift. Verified working rather than merely quiet: the hook exits 0 against the baseline and exits 1 on a canary file containing an AWS-shaped key. `.env.example` is deliberately no longer excluded — excluding it meant a real credential pasted into the template would never be caught.
- Fixed Transit Gateway attachment types being silently mislabelled (`TODO.md` → T-408). `_collect_tgw_attachments` derived the type as `AttachmentType(raw.replace("-", "_"))`, which mishandled two of the six values AWS actually returns: `direct-connect-gateway` became `direct_connect_gateway` and `tgw-peering` became `tgw_peering`, neither an enum member, so both fell through to `AttachmentType.VPC`. Every Direct Connect gateway attachment on a Transit Gateway was recorded as a VPC attachment — wrong in the topology, the diagrams, and any rule counting VPC attachments. Replaced with a declared `_TGW_ATTACHMENT_TYPES` mapping covering all six wire values; an unmapped value now logs a warning naming itself instead of failing silently. Found by writing the coverage T-408 exists to restore.
- Cleared the `ty` tail from 6 diagnostics to 2 (`TODO.md` → T-410), with no suppressions. `DrawioMCPClient.search_shapes` built an unannotated payload literal that infers `dict[str, str | int]`, which invariance rejects against `_post`'s `dict[str, object]` — now annotated, matching its sibling `create_diagram`. `EncyclopediaReportRenderer.render`, `.render_html`, and `build_encyclopedia_context` declared `findings: list[Finding | dict]` while every caller passes `list[Finding]`; all three take `Sequence[Finding | dict]` now, which is covariant and is all the code needs, since the parameter is only iterated. `RenderPipeline.run()` re-read `self.store.engagement_id` (`str | None`) at five sites instead of the `self.engagement_id: str` its own constructor guard had already narrowed. The two remaining diagnostics are the T-413 SDK stub artifact on `AIProjectClient.agents`, verified working at runtime, so the `ty` pre-commit hook stays non-gating.
- Pinned `azure-ai-projects` back to `>=1.0.0,<2` and declared `azure-ai-agents>=1.1.0,<2` (`TODO.md` → T-413). `FoundryAgentClient` drives `agents.threads`, `.messages`, and `.runs`, which is the 1.x surface; 2.x replaced it entirely, so every call raised `AttributeError` against the previously-declared `>=2.3.0` — disguised as a generic `FoundryAgentError` by a broad `except Exception`. This is live code, reached whenever `RecommendationEngine` builds its own agent client. Pinning alone was insufficient: in 1.x `AIProjectClient.agents` returns an `AgentsClient` from the separate, undeclared `azure-ai-agents` package, which is what actually provides those operations.
- Cleared the remaining Optional-dereference and annotation defects found by `ty` (`TODO.md` → T-410), taking it from 71 to 60 diagnostics. `AzureDiscovery`'s credential and subscription client are narrowed through `_cred`/`_subs` accessors across 17 read sites; `DiagramExporter._drawio_bin` can no longer reach `subprocess.run` as a `None` argv[0]; `AnalysisEngine.run()` gained the same `engagement_id` guard as the discovery paths; and `RenderPipeline` now requires a bound store at construction, because `DeliverableManifest` is an unvalidated dataclass that was writing a `null` engagement id into the manifest. `render_tgw_topology`'s return annotation was corrected — it nests TGW id → format → Path, unlike its single-diagram siblings.
- Hardened three Optional-dereference paths found by `ty` (`TODO.md` → T-410). `AWSDiscovery._mgmt_session` is `Session | None` until `run()` creates it, and every per-account helper dereferenced it directly; a `_mgmt` property now narrows it once and raises a named `RuntimeError` rather than surfacing as `AttributeError` on `NoneType` several frames deeper. Both discovery `run()` methods passed `self.store.engagement_id` (`str | None`) into models requiring `str`, and now fail early with a `ValueError` naming the fix. `PortalGenerator._filesizeformat` divided its own `int` parameter into a float, and now uses a local.
- Fixed an `AttributeError` in the Azure peering/gateway-transit rule (`TODO.md` → T-410). `analysis_engine._check_azure_peering_gateway_transit()` read `gw.vnet_id`, but `AzureVirtualNetworkGateway` has no such field — it carries `subnet_id`. The rule raised for any subscription that actually had a gateway, so it only ever ran clean when the gateway list was empty. It now matches on the ARM id nesting (`<vnet_id>/subnets/<name>`), which is the real link between a gateway and its VNet.
- Fixed a `TypeError` when rendering the technical report (`TODO.md` → T-410). `_build_context()` called `sorted()` over a set of `Finding.account_id`, declared `str | None`, so a single finding without an account id raised "'<' not supported between instances of 'str' and 'NoneType'". Both comprehensions now filter falsy ids.
- Fixed `EngagementStore.init()` and `save()`, which raised `TypeError` and so could never create or persist an engagement (`TODO.md` → T-410). Both called `config.model_dump(mode="json", default=str)`, but Pydantic v2's `model_dump` has no `default` parameter — and the argument was redundant anyway, since `_atomic_write` already passes `default=str` to `json.dump` and `mode="json"` renders datetimes and enums. The full `init` → `save` → `load` cycle now works.
- Fixed a `TypeError` that aborted AWS discovery at the first region (`TODO.md` → T-410). `aws_discovery.py` called `write_discovery_checkpoint(engagement_id, "aws", account_id=..., data=...)`, but that method takes `(engagement_id, platform, account_or_sub_id, data)` — so `account_id=` was an unexpected keyword and the required parameter was missing. The call sits inside the per-region loop, so no AWS checkpoint was ever written. Azure's equivalent call was always correct. Covered by `TestRegionCheckpointWrite` against a real `EngagementStore`.
- Added the missing `subnet_type` field to `AzureSubnet` (`TODO.md` → T-410). The Azure discovery path already passed `subnet_type=_classify_subnet(...)`, but the model had no such field and Pydantic discards extras by default, so every classification that function computed was silently thrown away — and it exists for no other purpose. The AWS `Subnet` model always had the field. Defaulted to `SubnetType.UNKNOWN` so existing checkpoints still deserialise.
- Fixed six type defects found by `ty` (`TODO.md` → T-410), taking it from 98 to 92 diagnostics. `analysis_engine.py` used the builtin `callable` in a type expression and `options: AnalysisOptions = None`; `render_pipeline.py` had the same non-Optional default plus `Path(...) / store.engagement_id` where that value is `str | None`, which now raises a `ValueError` naming the missing input instead of an opaque `TypeError`; `technical_report.py` called `.startswith()` on `Finding.resource_type`, declared `str | None` with default `None`, in two places; and `core/throttle.py` could `raise last_exc` while it was `None`. Verified behaviourally as well as by re-running the checker.
- Fixed a guaranteed `TypeError` in the AWS discovery access-denied path, found by `ty` on its first run (`TODO.md` → T-402). `aws_discovery.py` called `store.write_audit_event(engagement_id, "aws", event={...})`, but that method takes `(engagement_id, event)` — `"aws"` bound to `event`, so the keyword raised `TypeError: got multiple values for argument 'event'`. It was the only call site and sits in the branch that fires whenever an account cannot be assumed, which is routine in org-wide discovery. The cloud label now travels inside the event dict, covered by `TestAccessDeniedAuditEvent` against a real `EngagementStore` rather than a mock.
- Repaired `.secrets.baseline`, which was invalid JSON and had been for some time — a `]` where a `}` belonged on the `TwilioKeyDetector` entry broke the `plugins_used` array, so any tool reading it failed with "Unable to read baseline" (`TODO.md` → T-411).
- Removed the hardcoded AWS region from the discovery path (`TODO.md` → T-404). `_get_enabled_regions()` called `session.client("ec2", region_name="us-east-1")`; that literal does not exist in the `aws-us-gov` or `aws-cn` partitions, so region enumeration could never work there. The endpoint is now resolved by `_region_for_region_lookup()` — session's configured region first, then the first explicitly requested region, then the new declared `DiscoveryOptions.region_lookup_endpoint`, which callers in other partitions can override. A sweep of `cna/` confirms no other inline region literal, hardcoded account ID, or inline Azure location remains. The rule is now recorded in `README.md` → "Repository conventions".
- Corrected the fail-fast messages in `212-deploy-aws-split.yml`, which asserted something untrue (`TODO.md` → T-101). All three jobs claimed `infra/terraform/providers/aws/* has no resources yet`; the eight provider modules in fact declare 90 resources (ai 3, compute 25, database 2, identity 16, observability 22, runtime 10, security 4, storage 8). The real blocker is human account setup, so the messages now name `REVIEW.md` R-001 (AWS account), R-002 (S3 state backend + DynamoDB lock table), and R-003 (OIDC deploy role in CI secrets), and point at T-501 for the implementation. Fail-fast behaviour and the `policy-gates → plan → apply` job shape are unchanged, and the `plan` and `apply` messages are now job-specific rather than three copies of one string.
- Removed the six placeholder defaults from the AWS Terraform GitHub variables (`TODO.md` → T-202). `github_owner` and `github_repository` defaulted to `<GITHUB_OWNER_PLACEHOLDER>` and `<GITHUB_REPO_PLACEHOLDER>` in the identity module and both workload roots — syntactically valid, semantically wrong, and would have applied successfully to produce a deploy role nobody could assume. The defaults are gone, so Terraform now refuses to plan without real values; no literal `PLACEHOLDER` string remains under `infra/`.
- Fixed `deepmerge-ts` stack exhaustion on recursive object graphs, Dependabot alert #30 (GHSA-ggr8-5vv4-36mx / CVE-2026-40345, high, CVSS 4.0 8.2) — `TODO.md` → T-204 follow-up. Pinned to `^8.0.1` via `overrides` (patched in 8.0.0). The package reaches the tree only through `prisma` → `@prisma/config@6.19.3`, which pins it at exactly 7.1.5, and the Prisma 6.20 prerelease still pins 7.1.5, so an override is the only available route. Notably `npm audit` reported 0 vulnerabilities while this advisory was live — it was published the same day and the npm registry advisory database had not yet carried it, so a clean `npm audit` is necessary but not sufficient. Verified with `prisma generate`, `prisma version`, `prisma migrate deploy --help`, `prisma validate`, a full `next build`, and a direct check that the advisory's self-referencing-object case merges without stack exhaustion on 8.0.1.
- Remediated the open Dependabot advisories in the `apps/cna-web` npm tree (`TODO.md` → T-204); `npm audit` goes from 4 vulnerable packages (3 high, 1 moderate) to 0. `next` 16.2.11 → 16.3.1 with `eslint-config-next` to match, and the `postcss` floor raised to `^8.5.23` (resolves 8.5.26), closing the sourceMappingURL path-traversal advisory GHSA-6g55-p6wh-862q and its incomplete-fix follow-up. `nanoid` 3.3.12 → 3.3.18 (infinite loop on zero or negative size) and `brace-expansion` 1.1.16 → 1.1.18 / 5.0.7 → 5.0.9 (unbounded-expansion DoS, CVE-2026-14257 and its mitigation bypass) are pinned through targeted `overrides` rather than a blanket tree refresh. `pip-audit` over `pyproject.toml` reports no known vulnerabilities, so the Python dependency set was not implicated. Verified with `npm ci --legacy-peer-deps`, `tsc --noEmit`, and a full `next build`.
- Fixed `scripts/validate_module_deps.py` silently passing from any working directory other than the repository root (`TODO.md` → T-406): `MODULES_DIR` was the relative path `cna/modules`, so running the guard from elsewhere found no `module.yaml` files and reported `OK: 0 installed modules`. It now resolves from `__file__` and fails when the modules directory is missing or holds zero modules, rather than treating "nothing found" as a pass.
- Fixed `scripts/validate_documentation_model.py` enforcing only half the four-document model (`TODO.md` → T-605): it reported extra documents but never checked the four required ones existed, so deleting `README.md`, `CHANGELOG.md`, `REVIEW.md`, and `TODO.md` in one commit left the guard passing. Missing and extra documents are now both reported in a single run.
- Fixed the same guard missing mixed-case and non-`.md` documentation (`TODO.md` → T-606): the scan was a case-sensitive `rglob("*.md")`, so `docs/ROADMAP.MD`, `NOTES.Md`, `docs/NOTES.markdown`, and `docs/PLAN.rst` all passed. It now matches a case-folded suffix set covering 13 renderable extensions, and compares root allow-list membership case-insensitively.
- Fixed the same guard failing on gitignored build artifacts (`TODO.md` → T-608): it walked the filesystem, so a normal `pytest` run — which writes `.pytest_cache/README.md` — made the guard fail with advice that made no sense for a tool cache. Candidates now come from `git ls-files`, matching the rule being enforced, since untracked scratch is not part of the repository.
- Aligned the same guard's diagnostics with its sibling (`TODO.md` → T-607): violations now print to `stderr`, matching `validate_shape_catalog.py`, so both guards in the same CI job report on the same stream.
- Corrected stale workflow references left over from the workflow renumbering (`TODO.md` → T-102): `.env.example` pointed at a non-existent `110-sync-keys.yml` (now `340-sync-keys.yml`) and listed retired workflow numbers as variable consumers (now 000, 100, 211, 220, 330–360), and `scripts/Initialize-CnaGitHubSecrets.ps1`'s bootstrap report referenced `210-deploy-azure.yml` and `110-sync-keys.yml` (now `211-deploy-azure-split.yml` and `340-sync-keys.yml`).
- Fixed Key Vault Firewall Circumvention in `211-deploy-azure-split.yml` by dynamically adding/removing the GitHub runner's public IP.
- Fixed Missing Azure CLI Version Pinning in `211-deploy-azure-split.yml` by injecting an installation step.
- Removed fragile Private IP guessing script since Terraform now natively manages the PostgreSQL private DNS zone.
- Replaced pipeline `gh variable set` mutation with the Terraform GitHub Provider for managing repository variables.
- Removed out-of-band Key Vault certificate import workflow step since certificates are now rotated natively in Terraform.
- Fixed a rate-limit bypass on the local admin login (`apps/cna-web/app/api/local-admin/route.ts`): the per-IP brute-force guard keyed off the first, client-spoofable hop of `X-Forwarded-For`; it now keys off `X-Azure-ClientIP`, which Azure Front Door sets itself and clients can't override.
- Added `.reports/` to `.gitignore` — the bootstrap script's one-time plaintext local-admin password report was writing to a path that wasn't excluded from version control.

### Changed
- Re-measured the `ty` backlog with dependencies actually installed (`TODO.md` → T-410). A bare container reports 49 diagnostics; `pip install -e .` drops that to **24**, since all nine declared-but-uninstalled packages resolve. The remainder is 15 `unresolved-import` — every one an *undeclared* dependency, now tracked as T-414 — and 8 real findings. Two new problems were only visible once the SDKs were present and are filed as T-413 and T-414.
- Guarded five scaffold CLI commands with `NotImplementedError` (`TODO.md` → T-412): `cna analyze`, `cna report generate`, `cna report preview`, `cna publish run`, and `cna publish status`. All five called `EngagementStore` methods that do not exist and died with an unhandled `AttributeError` — in `analyze`'s case inside an `except FileNotFoundError` that could not catch it — with nothing marking them unfinished. Each guard names the exact missing methods, so the failure says what would have to be built.
- Documented why the GitHub OIDC provider carries no `thumbprint_list` (`TODO.md` → T-203). Verified first: no thumbprint value exists anywhere under `infra/` — the legacy placeholder survives only in the superseded `migrate/` root, which T-401 retires. The comment above `aws_iam_openid_connect_provider.github` now records why the omission is correct (AWS provider 5.x validates GitHub's endpoint against its trusted CA store), that the `migrate/` placeholder must not be copied in, and that AWS permits exactly one GitHub OIDC provider per account — with the `terraform import` command for when the target account already has one, since a duplicate create fails the entire apply.
- Refreshed both pinned container base image digests, the Docker half of the dependency surface that neither `npm audit` nor `pip-audit` covers (`TODO.md` → T-204). `node:24-alpine` `a0b9bf06…` → `d32cdf61…` (all four stages of `apps/cna-web/Dockerfile`) and `python:3.14-slim` `cea0e604…` → `ce407646…` (the root `Dockerfile`, `apps/cna-api`, and `apps/cna-worker`) — ten `FROM` lines across four Dockerfiles. Both tags had been rebuilt upstream since the previous pins, and upstream rebuilds of these images are typically OS package updates. Each new digest was resolved from the registry twice and confirmed to be an OCI image index carrying `linux/amd64`.
- Added `.codex` to `[tool.ruff] extend-exclude` in `pyproject.toml` (`TODO.md` → T-607). `README.md` → "Repository conventions" and the guard's docstring both claimed `.claude/`, `.agents/`, and `.codex/` were excluded from lint; only the first two were listed. Adding the exclusion makes both statements true and holds if `.codex/` ever gains Python.
- Moved live Azure prerequisite, image build, deploy, runtime, teardown, beta acceptance, and cost/footprint validation onto the numbered GitHub Actions workflows and their deployment evidence artifacts, rather than a hand-maintained checklist.
- Aligned MCP configuration defaults on `https://mcp.azure.com`, `https://aws-mcp.us-east-1.api.aws/mcp`, and `streamable-http` transport.
- Signed off on the narrowed Front Door WAF `/auth/*` exclusion policy ([#111](https://github.com/saulpatinojr/Work-Cloud_Network_Assessment/issues/111)) and the break-glass local admin login ([#112](https://github.com/saulpatinojr/Work-Cloud_Network_Assessment/issues/112)); `frontdoor_waf_mode` now defaults to `Prevention` in both dev and prod (previously `Detection`, pending review).
- Renamed `scripts/cleanup-stale-ai-resources.ps1` to `scripts/Remove-CnaStaleAiResources.ps1` to follow PowerShell's Verb-Noun naming convention, matching `scripts/Initialize-CnaGitHubSecrets.ps1`.
- Completed the Python code review that gated issue #110 (#140): `ruff check` and `ruff format --check` are clean across `cna/` with no ignore-list widening, boto3 and azure-mgmt client usage was reviewed for consistency across the AWS and Azure discovery paths, and coverage was assessed against the 80% threshold. A runtime-breaking `with_retry` contract bug in the AWS discovery path was found and fixed; the remaining consistency, typing, and coverage gaps were recorded as engineering work.
- Completed the dead and stale code sweep that gated issue #110 (#139): four verified-dead items were removed from `apps/cna-web/` (221 lines), `cna/` and `infra/` were surveyed without further deletions, and no `TODO`/`FIXME` comment was found referencing a closed issue.
- Consolidated repository documentation onto four authoritative documents — `README.md` (repository purpose), `CHANGELOG.md` (completed work), `REVIEW.md` (human-resolvable blockers), and `TODO.md` (engineering work queue) — with the GitHub Wiki as the destination for all long-form documentation. `README.md` was reduced to purpose, quick start, configuration, and navigation; `AWS_SETUP_TODO.md` and `REVIEW_TASK.md` were absorbed into `REVIEW.md`, `TODO.md`, and this changelog and removed.

### Removed
- Removed the Anthropic / Foundry-Claude inference path end-to-end (#100): the `foundry-claude` engine and its wire format in `apps/cna-web/lib/ai-engine.ts` and `cna/ai_engine/chat_agent.py`, the `foundry_claude_*` Terraform variables/outputs and `FOUNDRY_CLAUDE_*` container env vars (dev + prod), and the `FOUNDRY_CLAUDE_*` references in `.env.example`, the bootstrap script, and the drift/sync workflows. Azure OpenAI (`gpt-chat-latest`) is now the only supported engine. See ADR-0001 in the GitHub Wiki.
- Removed the Foundry private-access smoke test (#98/#99): `scripts/ci/validate_foundry_private_access.sh`, its `211-deploy-azure-split.yml` step, and the associated evidence wiring. See ADR-0002 in the GitHub Wiki.
- Removed the deploy-time "Sync Entra app web URLs" step (#101); the NextAuth redirect URI is owned by the Graph-capable bootstrap script on the application object, keeping the deploy identity least-privilege. See ADR-0003 in the GitHub Wiki.
- Removed `AWS_SETUP_TODO.md` and `REVIEW_TASK.md` from the repository root after migrating their content into `REVIEW.md`, `TODO.md`, and this changelog.
- Removed `infra/terraform/MIGRATION-workspace-to-platform.md` and the non-root `migrate/README.md` after migrating their content into `TODO.md` — the Log Analytics workspace state-migration runbook into T-304, and the superseded `migrate/` root's assessment-level IAM policy mapping and directory inventory into T-401. Both are queued for the GitHub Wiki, which is their proper destination; `TODO.md` → T-601 publishes them and T-602 trims the inline copies once it has.

---

## [0.8.0-beta] - 2026-06-16

### Added
- Added Azure Front Door Premium private-origin hardening, private endpoint approval automation, Azure Firewall egress control, subnet-specific NSGs, centralized diagnostics, VNet flow logs, and Azure AI Foundry private access validation gates.
- Added network security ADFs for edge ingress, east-west segmentation, north-south egress, private PaaS connectivity, centralized logging, Front Door private origin, private endpoint approval automation, and virtual network flow logs.
- Added post-deploy Foundry validation from the web Container App to verify private DNS resolution and managed-identity inference.

### Changed
- Changed the project release posture from `1.0.0` to `0.8 beta` while beta validation remains open.
- Updated Terraform, workflows, and documentation to use `cna-*` naming, separate tfstate resource groups, and GitHub Secrets/Variables as the deployment source of truth.
- Updated the AI runtime decision model so Foundry Claude is the Terraform-managed private provider and Azure OpenAI remains optional and out-of-band unless promoted to a first-class Terraform-managed provider.
- Merged or manually landed all open Dependabot PRs and closed the remaining conflicted PRs after their exact dependency updates reached `main`.
- Confirmed GitHub has no open PRs and no open Issues for final beta sync.

### Validation
- `npm ci`, `npx prisma generate`, and `npm run build` passed after dependency updates.
- Terraform validation passed for Azure `dev` and `prod` after network hardening changes.
- Live Azure beta validation is handled by the numbered GitHub Actions deployment workflows and deployment evidence artifacts.

---

## [1.0.0-prep] - 2026-06-15

### Changed
- Standardized active Azure deployment naming on `cna-*` resources with `rg-cna-<env>-<region>` workload resource groups.
- Updated Terraform workload resource group naming and generic CNA tag defaults for customer deployments.
- Aligned deployment, workflow, cleanup, and secrets documentation with GitHub Secrets/Variables as the source of truth.
- Migrated the documentation content set to the GitHub Wiki with generated table-of-contents sections.
- Migrated ADRs to the GitHub Wiki, consolidated remaining gap/footprint notes into the release documentation, and removed obsolete local audit artifacts.

---

## [1.0.0] - 2026-05-28

### Added
- **AWS Provider Expansion (Phase G)**: Added AWS Network Firewall and WAF v2 collectors; rules `AWS-NET-012` and `AWS-NET-013` are now live.
- **CISA ZTMM v2 Framework Mappings**: All 30 rule emissions now include CISA Zero Trust Maturity Model v2 pillar and control mappings.
- **Defender for Cloud Recommendations**: Added `_collect_defender_assessments()` and mapped defender assessments on the unified topology.
- **App Gateway Capacity Metrics**: Collector #9 in `_collect_network_metrics` and rule `AZ-NET-019` are now active.
- **Client Portal Hardening**: Implemented `deleteBlob()`, `generateSasUrl()`, and `cleanupExpiredDeliverables()` to secure the delivery pipeline.
- **Active Roadmap Priorities**: Integrated the following future tasks into the roadmap:
  - MCP integration
  - Draw.io integration
  - AWS assessment rules expansion
  - UI Update for new badging
  - Replatform to AWS

### Changed
- **Sprint 5 WCAG 2.1 / W3C / OWASP Audit Mitigation**: Resolved all 47 findings across the Next.js frontend:
  - **Accessibiity (WAI)**:
    - [WAI-01] Added `aria-hidden` and `aria-label` tags to navigation SVG icons.
    - [WAI-02] Replaced `title` attributes with descriptive `aria-label`, `aria-expanded`, and `aria-controls` on the sidebar toggle.
    - [WAI-03] Replaced the custom `sp-help-modal.tsx` with a fully accessible native dialog modal.
    - [WAI-04/05] Added `aria-expanded` and `aria-controls` to findings group headers and instances rows.
    - [WAI-06] Replaced label-caps `<p>` elements with semantic `<h2>`/`<h3>` headings.
    - [WAI-07] Added `<caption>`, `scope="col"`, and `scope="row"` markup to risk matrix tables.
    - [WAI-08] Added non-color indicators (border/underline) and `aria-pressed`/`aria-checked` to severity filter toggle buttons.
    - [WAI-09] Fixed color contrast for `text-navy-400` to exceed 4.5:1 ratio on light and dark mode.
    - [WAI-10] Added global `focus-visible` ring indicators to `globals.css` for interactive components.
    - [WAI-11] Implemented `role="progressbar"` and `aria-valuenow` on discovery progress bars.
    - [WAI-12] Fixed `<details>`/`<summary>` accessibility bugs for Firefox and NVDA compatibility.
    - [WAI-13] Injected descriptive `aria-label` properties on proportional severity segments.
    - [WAI-14] Added `(opens in new tab)` accessibility text to links with `target="_blank"`.
    - [WAI-16] Added a hidden skip-to-main-content skip link for screen readers.
    - [WAI-17] Added appropriate `aria-label` descriptors to sidebar `<aside>` and `<nav>` landmarks.
    - [WAI-19] Spinner SVGs marked with `aria-hidden="true"` and paired with `sr-only` live regions.
    - [WAI-20/21/23] Forms and selects updated with standard `autocomplete` fields, thicker focus rings, and step indicators.
  - **Security (OWA)**:
    - [OWA-01] Configured nonce-based Content Security Policy (CSP) headers in `next.config.ts` using middleware.
    - [OWA-02] Added per-user API rate limiting (5 requests / 60s) to Server Actions (AI generation and discovery triggers).
    - [OWA-03] Wrapped `deleteEngagement` trigger inside standard `<form>` to enforce CSRF validation.
    - [OWA-04] Restricted NextAuth development-grade cookie fallbacks to non-production environments.
    - [OWA-05] Mitigated timing attacks on the discovery-jobs API by rejecting unauthenticated requests before database lookup.
    - [OWA-06] Implemented magic-byte validation (validating headers, not just extensions) for uploaded documents.
    - [OWA-08] Hardened `lib/auth.ts` to enforce `useSecureCookies: true` in production.
    - [OWA-09] Replaced default native `confirm()` triggers with custom accessible modal dialog confirmation elements.
    - [OWA-10] Added Zod schemas to parse and validate database progress logs.
    - [OWA-12] Mapped raw Azure SDK exception strings to user-friendly messages.
    - [OWA-13/14] Expanded security headers (modern `Permissions-Policy` controls and Strict-Transport-Security preload directives).
  - **W3C Standards (W3C)**:
    - [W3C-01/12] Replaced dual logo instances with unified `<picture>` tag wrapping `next/image` to prevent CLS and duplicate requests.
    - [W3C-02] Appended `aria-hidden="true" focusable="false"` to all decorative SVGs.
    - [W3C-03] Registered standard CSS scrollbar styles side-by-side with webkit engines.
    - [W3C-04] Scoped font-smoothing parameters to preserve ClearType rendering on Windows.
    - [W3C-05] Injected root `<meta name="color-scheme">` tags to prevent flash on load.
    - [W3C-06] Added `@supports (backdrop-filter: blur(1px))` queries for Firefox glass backfalls.
    - [W3C-08] Augmented TypeScript typings module to handle custom CSS property `--bar-pct`.
    - [W3C-09/10] Set `rel="noopener noreferrer"` and unique `aria-label` tags on all deliverable link forms.
- **Sprint 4 Hardening**:
  - Wired Spoke-VNet peerings to emit `AZ-NET-005` if missing local gateways.
  - Configured `AZ-NET-006` rule to alert when subnets lack Flow Log logging.
  - Added FQDN resolution validation against RFC 1918 ranges for Private Endpoints.
- **Sprint 3 Hardening**:
  - Pinned `node:20-alpine` and `gitleaks-action` references in Dockerfiles and test actions to secure SHA digests.
  - Mapped Azure Front Door WAF Policies and added `AZ-NET-018` for Detection-mode WAF configs.
- **Sprint 2 Hardening**:
  - Collected live firewall rules, SNAT exhaustion levels, gateway bandwidth usage, and VNet IP utilization.
  - Hooked Azure Cost Management API and Log Analytics Traffic Analytics for east-west bandwidth tracking.
  - Integrated compliance mappings for HIPAA, FedRAMP, PCI-DSS, and ISO 27001.
- **Sprint 1 Hardening**:
  - Patched `_collect_private_endpoints()` IP collections and `vpn_client_pools` list manipulation bugs.
  - Implemented Spoke-to-Spoke firewall bypass checks (`AZ-NET-012`) and diagnostic settings checks (`AZ-NET-013`).

---

## [0.1.0] - 2026-04-14

### Added
- **Unified 3-Container Platform**:
  - `cna-web`: Next.js 15 frontend panel.
  - `cna-api`: FastAPI internal processing broker.
  - `cna-worker`: Python background tasks engine.
- **Terraform IaC modules**: Provisioning templates for all Azure networking, AI, databases, and container instances.
- **Automated Workflows (000–031)**: Checklists, OIDC credentials sync, fast-redeploy pipelines, and automatic variable updating.
- **OIDC Identity**: NextAuth v5 linked to Microsoft Entra ID with Analyst, Reviewer, Client, and Admin roles.
- **Sync Groups & Topology Merge**: Run isolated per-subscription discovery jobs; merge resources under one global assessment profile.
- **Unique Findings Count**: Deduplicated findings counting in the portal page.
- **Assessment Deliverables**: Supported 5 distinct deliverable documents, including full self-contained `COMPREHENSIVE_ASSESSMENT` HTML formats.
- **MS Learn Enrichment**: Injected live Microsoft Learn documentation matches into AI analysis pipelines.
- **Presentation Dashboard**: Radar charts, maturity indicators, compliance maps, and interactive topologies.

---
