# CD: Publish portal workflow
# Triggered manually (workflow_dispatch) or on a workflow_call from another pipeline.
# Runs `cna publish run` inside the CNA container against a specified engagement.
#
# Required repository secrets:
#   CNA_AWS_ROLE_ARN     — IAM role ARN for CNA-Publish (OIDC)
#   CNA_PUBLISH_BUCKET   — S3 bucket name
#   (Azure variant: CNA_AZURE_CLIENT_ID, CNA_AZURE_TENANT_ID, CNA_AZURE_SUBSCRIPTION_ID)
#
# Design:
#   - Uses OIDC (no long-lived AWS keys stored as secrets)
#   - Engagement data is never checked into the repo
#     (engagements/ is in .gitignore)
#   - Runs in the same container image used locally for reproducibility
name: CD — Publish Portal

on:
  workflow_dispatch:
    inputs:
      engagement_id:
        description: 'Engagement ID (e.g. acme-20260305-a3f2)'
        required: true
        type: string
      cloud:
        description: 'Cloud storage target'
        required: true
        type: choice
        options: [aws, azure]
        default: aws
      image_tag:
        description: 'CNA image tag to use (default: latest)'
        required: false
        default: 'latest'
        type: string

permissions:
  contents: read
  id-token: write   # OIDC

jobs:
  publish:
    name: Publish — ${{ inputs.engagement_id }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      REGISTRY: ghcr.io
      IMAGE_NAME: ${{ github.repository }}

    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      # AWS OIDC — no long-lived keys
      - name: Configure AWS credentials (OIDC)
        if: inputs.cloud == 'aws'
        uses: aws-actions/configure-aws-credentials@e3dd6a429d7300a6a4c196c26e071d42e0343502  # v4.0.2
        with:
          role-to-assume: ${{ secrets.CNA_AWS_ROLE_ARN }}
          aws-region: us-east-1

      # Azure OIDC
      - name: Configure Azure credentials (OIDC)
        if: inputs.cloud == 'azure'
        uses: azure/login@a457da9ea143d694b8d37b84c16a66cf4db8aca3  # v2.3.0
        with:
          client-id: ${{ secrets.CNA_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CNA_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CNA_AZURE_SUBSCRIPTION_ID }}

      - name: Log in to GHCR
        uses: docker/login-action@9780b0c442fbb1117ed29e0efdff1e18412f7567  # v3.3.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull CNA image
        run: |
          docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ inputs.image_tag }}

      - name: Run cna publish
        run: |
          docker run --rm \
            -e AWS_ACCESS_KEY_ID -e AWS_SECRET_ACCESS_KEY -e AWS_SESSION_TOKEN \
            -e AZURE_CLIENT_ID -e AZURE_TENANT_ID -e AZURE_SUBSCRIPTION_ID \
            -v ${{ github.workspace }}/engagements:/app/engagements:ro \
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ inputs.image_tag }} \
            publish run \
              --engagement-id ${{ inputs.engagement_id }} \
              --cloud ${{ inputs.cloud }} \
              --bucket ${{ secrets.CNA_PUBLISH_BUCKET }} \
              --ttl-hours 168
